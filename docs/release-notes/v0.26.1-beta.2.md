# v0.26.1-beta.2: Fragnesia recordings and restart reliability

Changes since `v0.26.0`.

## Vul

### [cve-2026-46300](https://github.com/ctrsploit/ctrsploit/blob/v0.26.1-beta.2/vul/cve-2026-46300/README.md) ([#376], [#378], [#379])

Aliases: `46300`, `fragnesia`.

New:

- Adds the Fragnesia Linux kernel vulnerability module for the xfrm
  ESP-in-TCP page-cache write primitive.
- Adds conservative kernel prerequisite checks for CVE-2026-46300.
- Supports shared pipe-primitive exploit flows:
  - `privilege-escalate`
  - `image-pollution`
  - `clean`
  - `escape exec`
  - `escape image`
  - `escape restart`
- Adds Fragnesia-specific escape image writer support.
- Adds a Fragnesia-specific restart loader for `escape restart`.
- Adds demo SVGs for checksec, local privilege escalation, image pollution,
  clean, `escape exec`, generated escape images, and `escape restart`.
- Documents Fragnesia's 4096-byte collateral window and the resulting target
  constraints for structured files, image pollution, and runc overwrite flows.
- Adds README reproduction steps for checksec, local privilege escalation,
  image pollution, clean, `escape exec`, generated escape images, and
  `escape restart`.
- Adds the module to the `vul`, `exploit`, `checksec`, and kernel module
  listings.

Fix:

- Retries the Fragnesia receiver `accept` call when it is interrupted by a
  signal, improving `escape restart` reliability.

Test:

- Adds Fragnesia primitive and command-surface tests.
- Adds restart loader patching and execution tests.
- Adds dqd e2e coverage for privilege escalation, image pollution, clean,
  `escape exec`, `escape image`, and `escape restart`.

### README module tables ([#376], [#377], [#379], [bf1a4d8])

Improve:

- Adds CVE-2026-46300 to the root README and `vul/README.md` kernel module
  tables.
- Marks `video` as supported for CVE-2026-46300 in the vulnerability status
  list.
- Adds maintainer metadata to module README files for generated module catalog
  consumption.

## Pipe Primitive

### escape restart ([#378])

Improve:

- Extends the shared restart escape path with Fragnesia restart loader support.
- Handles Fragnesia's prefix overwrite constraints by writing a compact loader
  and shebang trampoline before triggering container restart.

### escape image ([#378])

Improve:

- Adds Fragnesia extra writer files to generated escape images so the image
  payload can run the CVE-specific page-cache write helper.

## Build

### Makefile generate targets ([#378])

Improve:

- Moves generate-related targets into `make/Makefile.generate`.
- Keeps the top-level `Makefile` focused on including build, docs, generate,
  e2e, and help fragments.
- Adds the CVE-2026-46300 restart-loader generation rule.

### [modules.json](https://github.com/ctrsploit/ctrsploit/blob/v0.26.1-beta.2/modules.json) ([#375], [#377])

New:

- Adds a generated module catalog at `modules.json`.
- Adds `script/update_modules_json.go` for generating the catalog from module
  metadata.
- Adds `script/update_module_maintainers.py` for updating README maintainer
  front matter.

Test:

- Adds module catalog generation tests.

## Env

### [services](https://github.com/ctrsploit/ctrsploit/blob/v0.26.1-beta.2/env/services/README.md) ([#377])

Improve:

- Adds maintainer metadata and standalone README content for the `services`
  environment command.

## E2E

### [cve-2026-46300 e2e](https://github.com/ctrsploit/ctrsploit/blob/v0.26.1-beta.2/vul/cve-2026-46300/e2e.yml) ([#376], [#378], [#379])

New:

- Adds the CVE-2026-46300 dqd e2e manifest.
- Covers `cve-2026-46300-privilege-escalate`,
  `cve-2026-46300-image-pollution`, `cve-2026-46300-clean`,
  `cve-2026-46300-escape-exec`, `cve-2026-46300-escape-image-start`,
  `cve-2026-46300-escape-image-exec`, and
  `cve-2026-46300-escape-restart`.

## Skills And Agent Docs

### [record-terminal-session](https://github.com/ctrsploit/ctrsploit/blob/v0.26.1-beta.2/.opencode/skills/record-terminal-session/SKILL.md) ([#379])

Improve:

- Adds recording validation guardrails for baseline checks, local binary shape,
  failure triage, and destructive lab reset handling.

## Contributors

@ssst0n3

[#375]: https://github.com/ctrsploit/ctrsploit/pull/375
[#376]: https://github.com/ctrsploit/ctrsploit/pull/376
[#377]: https://github.com/ctrsploit/ctrsploit/pull/377
[#378]: https://github.com/ctrsploit/ctrsploit/pull/378
[#379]: https://github.com/ctrsploit/ctrsploit/pull/379
[bf1a4d8]: https://github.com/ctrsploit/ctrsploit/commit/bf1a4d8
